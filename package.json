{
  "name": "fusion-consumer",
  "version": "0.0.1",
  "description": "Consumer provides a set of types for reading and processing data from fusion data producer",
  "main": "index.js",
  "scripts": {
    "build": "tsup src/*.ts --format cjs --dts --minify --clean --sourcemap"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "@types/node": "^20.8.9",
    "tsup": "^7.2.0"
  },
  "dependencies": {
    "nekoton-wasm": "^1.3.1",
    "protobufjs": "^7.2.5"
  }
}
